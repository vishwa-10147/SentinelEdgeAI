Centralized deployment helpers

This folder groups quick install and run helpers for target devices.

- `pi/` — helper scripts and notes for Raspberry Pi installs (uses `scripts/setup_pi.sh`).
- `jetson/` — helper scripts and notes for NVIDIA Jetson-style installs (venv + deps).

Use the subfolders for device-specific steps.
