[Unit]
Description=Notifier for integration_test failures (example)
After=network.target

[Service]
Type=oneshot
User=vishwa
Environment=INTEGRATION_ALERT_WEBHOOK=
ExecStart=/usr/local/bin/notify_on_failure.sh integration_test.service

[Install]
WantedBy=multi-user.target
