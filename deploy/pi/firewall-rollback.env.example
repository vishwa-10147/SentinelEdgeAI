# Copy to /etc/sentinel/firewall-rollback.env and edit before enabling the timer.

ENABLE_ROLLBACK_GUARD=1
REPO_DIR=/home/vishwa/SentinelEdgeAI
VENV_PYTHON=/home/vishwa/SentinelEdgeAI/.venv/bin/python
HEALTH_URL=http://127.0.0.1:9000/api/health
FAIL_THRESHOLD=3

# Required only when DASHBOARD_API_KEY is set for the dashboard API.
#DASHBOARD_API_KEY=replace_with_dashboard_key
